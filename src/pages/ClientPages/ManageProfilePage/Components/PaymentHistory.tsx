import { List } from 'antd';

const PaymentHistory = () => {
    return (
        <div>
            <List itemLayout="vertical" size="default"></List>
        </div>
    );
};

export default PaymentHistory;
